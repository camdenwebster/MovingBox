# Image files
# Only track snapshot test images with LFS
MovingBoxTests/__Snapshots__/**/*.png filter=lfs diff=lfs merge=lfs -text
# Keep app icons and other assets as regular Git files
*.appiconset/**/*.png -filter -diff -merge text
Assets.xcassets/**/*.png -filter -diff -merge text
MovingBoxTests/__Snapshots__//*.png filter=lfs diff=lfs merge=lfs -text
